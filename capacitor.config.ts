import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'desing-token',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
